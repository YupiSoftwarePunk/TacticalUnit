# Документация разработчика: TacticalUnit Frontend часть

Добро пожаловать в команду разработки веб-платформы для игрового клана Squad. Данный документ поможет вам настроить окружение и запустить проект локально.

---

## Структура проекта
Данная платформа разделена на два независимых репозитория:
* **Frontend (текущий)**: Интерфейс пользователя на TypeScript + Next.js.
* **Backend**: API на .NET 10, управление базой данных и бизнес-логика. 
    * [Ссылка на репозиторий бэкенда](https://github.com/NekichNet/ACCS)

---

## Системные требования

Перед началом убедитесь, что у вас установлены следующие инструменты:

* **Node.js**: версия `20.x` или `22.x` (LTS).
* **Пакетный менеджер**: `npm`.

---

## Frontend

Стек: **Next + TypeScript + Tailwind CSS 4**.
Архитектура: **Feature-Sliced Design (FSD)**.

### Настройка и запуск
1. Перейдите в директорию клиента:
   ```bash
   cd client
   ```
2. Установите все зависимости:
   ```bash
   npm i
   ```
3. Запустите проект в режиме разработки:
   ```bash
   npm run dev
   ```
   *Приложение будет доступно по адресу `http://localhost:3000`.*

### Важные нюансы для разработки
* **Пути (Aliases)**: Используйте префикс `@/` для импортов. Например: `import { Button } from '@/shared/ui'`. Это настроено в `tsconfig.json`.
* **Стили**: Мы используем **Tailwind CSS**. Основные настройки темы и кастомные переменные (шрифты, цвета) находятся в `src/app/globals.css` (используется новый синтаксис `@theme`).
* **Шрифты**: В проекте используются шрифты `Capture It`, `Stengazeta` и `Nunito`, подключенные через переменные окружения в CSS.
* **Структура FSD**: Придерживайтесь деления на слои (`app`, `widgets`, `features`, `entities`, `shared`). Помните о правилах импорта: вышележащие слои не могут наследоваться от нижележащих.

---

## Командный воркфлоу (Git)

Чтобы проект оставался чистым, придерживайтесь следующих правил:

1. **Ветки**: Создавайте новую ветку для каждой задачи: `feature/task-name` или `fix/issue-name`.
2. **Фиксации**: Пишите понятные сообщения на английском, например: `feat: add user profile page` или `fix: resolve auth token issue`.

---

## Полезные команды

| Задача | Команда |
| :--- | :--- |
| **Установка** | `npm i` |
| **Запуск** | `npm run dev` |
| **Сборка** | `npm run build` |
| **Линтер** | `npm run lint` |

---

> !IMPORTANT
> **Секреты и ключи**: Никогда не пушьте файлы `.env` или `appsettings.json` с реальными паролями в репозиторий. Используйте шаблоны `.env.example`.